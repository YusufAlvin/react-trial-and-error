// import PropTypes from 'prop-types';
import React from 'react';
import './Well.scss';

const Well = ({col, row, id}) => {
  return (
    <div className="well">
      <>{id}</>
    </div>
  );
};

// Well.propTypes = {

// }

// Well.defaultProps = {

// }

export default Well;
